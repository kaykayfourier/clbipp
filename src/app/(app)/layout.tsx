// TODO: render <BottomTabBar /> here once Person C ships components/layout/bottom-tab-bar.tsx
export default function AppLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
